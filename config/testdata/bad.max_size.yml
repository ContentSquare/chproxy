caches:
  - name: "longterm"
    dir: "cache_dir"
    max_size: "10p"

server:
  http:
    listen_addr: ":8080"

users:
  - name: "dummy"
    to_cluster: "cluster"
    to_user: "default"

clusters:
  - name: "cluster"
    nodes: ["127.0.1.1:8123"]