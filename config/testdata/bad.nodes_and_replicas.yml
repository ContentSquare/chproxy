server:
  http:
    listen_addr: ":8080"

users:
  - name: "default"
    to_cluster: "second cluster"
    to_user: "default"

clusters:
  - name: "second cluster"
    nodes: ["foo"]
    replicas:
      - name: "bar"
        nodes: ["bar"]
