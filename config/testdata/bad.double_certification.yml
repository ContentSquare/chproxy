server:
  https:
    cert_file: "cert_file"
    key_file: "key_file"
    autocert:
      cache_dir: "cache_dir"

users:
  - name: "default"
    to_cluster: "cluster"
    to_user: "default"

clusters:
  - name: "cluster"
    nodes: ["127.0.1.1:8123"]