server:
  https:
    listen_addr: ":443"
    autocert:
      cache_dir: "cache_dir"

users:
  - name: "dummy"
    to_cluster: "cluster"
    to_user: "default"

clusters:
  - name: "cluster"
    nodes: ["127.0.1.1:8123"]