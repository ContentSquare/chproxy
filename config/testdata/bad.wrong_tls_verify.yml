server:
  http:
    listen_addr: ":8080"
  proxy:
    skip_tls_verify: "true"

users:
  - name: "default"
    to_cluster: "second cluster"
    to_user: "default"

clusters:
  - name: "second cluster"
    scheme: "https"
    nodes: ["127.0.1.1:8123"]
